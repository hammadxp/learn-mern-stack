console.log("Hello World!");

const amount = 19;
const threshold = 20;

if (amount > threshold) console.log(`${amount} is greater than ${threshold}`);
else console.log(`${amount} is less than ${threshold}`);
